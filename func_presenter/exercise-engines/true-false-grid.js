// APP-ANCHOR: True-false grid exercise flow.
function startTrueFalseGrid(exercise) {
  state.exercise = exercise;
  state.trueFalseAnswered = 0;
  state.trueFalseScore = 0;
  state.trueFalsePageIndex = 0;
  state.trueFalsePageAnswered = 0;
  if (exercise.buildPages) {
    state.trueFalsePages = exercise.buildPages();
    state.trueFalseQuestions = state.trueFalsePages.flatMap((p) => p.questions);
  } else {
    state.trueFalseQuestions = exercise.buildQuestions(state.module);
    const pageSize = exercise.pageSize || state.trueFalseQuestions.length;
    state.trueFalsePages = Array.from({ length: Math.ceil(state.trueFalseQuestions.length / pageSize) }, (_, index) => ({ questions: state.trueFalseQuestions.slice(index * pageSize, (index + 1) * pageSize), imagePath: exercise.imagePath }));
  }
  hideAllScreens(); els.trueFalse.classList.remove("hidden"); els.trueFalse.dataset.exerciseId = exercise.id || "";
  els.trueFalseGrade.textContent = `GRADE ${state.grade}`; els.trueFalseScore.textContent = "0";
  els.trueFalseNext.textContent = exercise.continueLabel || (exercise.buildPages ? "CONTINUE" : "NEXT 8 STATEMENTS");
  els.trueFalseExit.textContent = "EXIT"; els.trueFalseInstruction.textContent = exercise.instruction || "READ EACH STATEMENT AND MARK IT TRUE OR FALSE.";
  const firstImage = state.trueFalsePages[0]?.imagePath || state.trueFalsePages[0]?.questions?.find((question) => question.imagePath)?.imagePath;
  els.trueFalseVisualPanel.classList.toggle("hidden", !firstImage); els.trueFalseCard.classList.toggle("with-visual", Boolean(firstImage));
  if (firstImage) setTrueFalseVisual(firstImage); renderTrueFalsePage();
}
function setTrueFalseVisual(imagePath) { if (!imagePath) return; els.trueFalseVisualBrief.textContent = state.exercise?.visualBrief || "A visual for this statement."; els.trueFalseVisualFallback.classList.add("hidden"); els.trueFalseImage.classList.remove("hidden"); els.trueFalseImage.alt = state.exercise?.visualBrief || "True or false visual"; els.trueFalseImage.src = imagePath; els.trueFalseVisualPanel.classList.remove("hidden"); els.trueFalseCard.classList.add("with-visual"); }
function renderTrueFalsePage() { const page = state.trueFalsePages[state.trueFalsePageIndex]; state.trueFalsePageAnswered = 0; els.trueFalseProgress.textContent = `${state.trueFalseAnswered} / ${state.trueFalseQuestions.length}`; els.trueFalseActions.classList.add("hidden"); els.trueFalseActionMessage.classList.add("hidden"); els.trueFalseNext.classList.remove("hidden"); els.trueFalseExit.classList.add("hidden"); const pageImage = page.imagePath || page.questions.find((question) => question.imagePath)?.imagePath; if (pageImage) setTrueFalseVisual(pageImage); els.trueFalseGrid.replaceChildren(...page.questions.map(createTrueFalseRow)); }
function createTrueFalseRow(question) { const row = document.createElement("article"); row.className = "true-false-row"; row.dataset.answer = question.answer; const statement = document.createElement("div"); statement.className = "true-false-statement"; if (question.imagePath) { const imageButton = document.createElement("button"); imageButton.type = "button"; imageButton.className = "true-false-image-button"; imageButton.textContent = "▣"; imageButton.setAttribute("aria-label", "Show statement image"); imageButton.addEventListener("click", () => setTrueFalseVisual(question.imagePath)); statement.append(imageButton); } const sentence = document.createElement("p"); sentence.textContent = question.prompt; statement.append(sentence); const actions = document.createElement("div"); actions.className = "true-false-actions"; for (const [answer, className, label, ariaLabel] of [["true", "true-button", "✓", "True"], ["false", "false-button", "✕", "False"]]) { const button = document.createElement("button"); button.type = "button"; button.className = `true-false-button ${className}`; button.textContent = label; button.setAttribute("aria-label", ariaLabel); button.addEventListener("click", () => answerTrueFalse(row, answer)); actions.append(button); } row.append(statement, actions); return row; }
function answerTrueFalse(row, selectedAnswer) { if (row.dataset.answered) return; row.dataset.answered = "true"; const isCorrect = selectedAnswer === row.dataset.answer; [...row.querySelectorAll("button")].forEach((button) => { button.disabled = true; const buttonAnswer = button.classList.contains("true-button") ? "true" : "false"; if (buttonAnswer === row.dataset.answer) button.classList.add("correct"); else if (buttonAnswer === selectedAnswer) button.classList.add("wrong"); }); row.classList.add(isCorrect ? "answered-correct" : "answered-wrong"); row.classList.add(row.dataset.answer === "true" ? "statement-true" : "statement-false"); if (isCorrect) state.trueFalseScore += 1; state.trueFalseAnswered += 1; state.trueFalsePageAnswered += 1; els.trueFalseScore.textContent = state.trueFalseScore; els.trueFalseProgress.textContent = `${state.trueFalseAnswered} / ${state.trueFalseQuestions.length}`; playFeedbackSound(isCorrect); if (state.trueFalsePageAnswered !== state.trueFalsePages[state.trueFalsePageIndex].questions.length) return; if (state.exercise.maxAnsweredBeforeExit && state.trueFalseAnswered >= state.exercise.maxAnsweredBeforeExit) { els.trueFalseActions.classList.remove("hidden"); els.trueFalseActionMessage.textContent = state.exercise.limitMessage || "There are still lots of different sentences, but it seems it's enough for now. Come back later."; els.trueFalseActionMessage.classList.remove("hidden"); els.trueFalseNext.classList.add("hidden"); els.trueFalseExit.classList.remove("hidden"); return; } if (state.trueFalsePageIndex < state.trueFalsePages.length - 1) { els.trueFalseActions.classList.remove("hidden"); els.trueFalseNext.classList.remove("hidden"); els.trueFalseExit.classList.remove("hidden"); return; } setTimeout(() => { hideAllScreens(); els.exerciseResultScore.textContent = `${state.trueFalseScore} / ${state.trueFalseQuestions.length}`; els.exerciseResultMessage.textContent = `${state.trueFalseScore} statements marked correctly.`; els.exerciseResult.classList.remove("hidden"); }, 700); }
function nextTrueFalsePage() { if (state.trueFalsePageIndex >= state.trueFalsePages.length - 1) return; state.trueFalsePageIndex += 1; renderTrueFalsePage(); }
